# Path to persist MongoDB data (default: ./mongo_data)
MONGO_DATA_PATH=./mongo_data
MONGO_USERNAME=votre_utilisateur_mongodb
MONGO_PASSWORD=votre_mot_de_passe_mongodb
JWT_SECRET=votre_secret_jwt_tres_long_et_aleatoire

# Frontend/Backend URLs
# Used by backend CORS: if unset, defaults to http://localhost:80
FRONTEND_URL=http://localhost:5173
API_URL=https://api-burn.grltt.fr

# Moodle configuration (used by server/utils/connectMoodle.js & testMoodleToken.js)
# Ensure to provide base URLs with trailing slash
MOODLE_BASE_URL=https://universitice.univ-rouen.fr/
CAS_BASE_URL=https://cas.univ-rouen.fr/cas/
MOBILE_URLSCHEME=moodlemobile

# Mail / IMAP configuration
IMAP_HOST=imap.univ-rouen.fr
IMAP_PORT=993
SOGO_URL=https://sogo.univ-rouen.fr/
# Allow self-signed certs only if needed (default true)
# IMAP_TLS_REJECT_UNAUTHORIZED=false

# RSA key persistence for mail password encryption
# Option A: provide PEM content directly (preferred for containers)
# MAIL_RSA_PRIVATE="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
# MAIL_RSA_PUBLIC="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----\n"
# Option B: file paths
# MAIL_RSA_PRIVATE_PATH=/keys/mail_private.pem
# MAIL_RSA_PUBLIC_PATH=/keys/mail_public.pem
# If neither provided, keys will be auto-generated into server_data/ on first run
# You can also change where on-disk keys are written with:
# KEYS_DIR=/data